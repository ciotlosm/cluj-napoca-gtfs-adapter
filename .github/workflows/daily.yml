name: "Daily build → binaries"

# Builds output/cluj-napoca.gtfs.zip and pushes it to the `binaries` branch.
# Triggers: cron at 00:30 UTC (after Transitous's ~00:00 UTC import)
# + manual workflow_dispatch + push to main.

on:
  schedule:
    - cron: '30 0 * * *'
  workflow_dispatch:
  push:
    branches:
      - main
    paths-ignore:
      - '**/*.md'
      - 'docs/**'
      - '.gitignore'
      - 'LICENSE'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build feed
        run: node src/cli.js build
        env:
          TRANZY_API_KEY: ${{ secrets.TRANZY_API_KEY }}
          TRANZY_AGENCY_ID: '2'

      - name: CSV parse smoke test (full network)
        # Scrapes every CTP CSV, runs them through the production parser,
        # fails the build if any cell is unrecognized (i.e. the #15 fix
        # needs extending).
        run: npm run smoke:csv

      - name: GTFS-RT trip-ID parity check
        # Fetches the live RT feed and asserts trip_ids match our
        # canonical pattern. Fails loudly if the upstream RT feed
        # changes its ID scheme — that's what would silently break
        # the neary app's GTFS-RT JOIN otherwise.
        run: npm run smoke:rt
        env:
          RT_PARITY_URL: ${{ vars.RT_PARITY_URL }}
          # Fail the build if the RT feed is unreachable OR its trip_ids
          # don't match. CI-only — keep RT_PARITY_FAIL_ON_FETCH_ERROR=0
          # locally so dev iteration isn't blocked by transient outages.
          RT_PARITY_FAIL_ON_FETCH_ERROR: '1'

      - name: Publish to binaries branch
        run: |
          DATE=$(date -u +%Y-%m-%dT%H:%M:%SZ)
          REMOTE="https://x-access-token:${GITHUB_TOKEN}@github.com/${{ github.repository }}.git"
          DIR="/tmp/binaries"

          if ! git clone --single-branch --branch binaries --depth 1 "$REMOTE" "$DIR" 2>/dev/null; then
            # First-ever build: create as orphan.
            git clone --depth 1 "$REMOTE" "$DIR"
            cd "$DIR"
            git checkout --orphan binaries
            git rm -rf . >/dev/null
            cd -
          fi

          mkdir -p "$DIR/output"
          cp output/cluj-napoca.gtfs.zip "$DIR/output/cluj-napoca.gtfs.zip"

          cd "$DIR"
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            echo "no changes to publish"
          else
            git commit -m "pipeline: $DATE"
            git push origin binaries
          fi
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Smoke-test the published URL (next day)
        if: github.event_name == 'workflow_dispatch'
        run: |
          URL="https://raw.githubusercontent.com/ciotlosm/cluj-napoca-gtfs-adapter/binaries/output/cluj-napoca.gtfs.zip"
          echo "Validating $URL"
          curl -sI "$URL" | head -5